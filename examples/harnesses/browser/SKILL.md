# Browser Playwright Loopback Fixture

Exercise a deterministic same-origin form flow, handled dialog, expected popup, expected download, DOM/ARIA capture, network evidence, screenshots, and final-state assertions against an operator-owned loopback fixture.

This fixture does not call an LLM, public website, credentialed account, anti-bot service, or hosted browser. It establishes deterministic local Chromium integration only.
